Docked rest countdown with a draining amber bar and one round play/pause button.

```jsx
<RestTimer seconds={90} remaining={54} running onToggle={toggle} onSkip={skip} />
```

Only appears while a workout is active; it sits above the tab bar, not inside the scroll area.
