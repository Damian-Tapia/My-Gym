Circle glyph, short title, one plain sentence, at most one secondary button.

```jsx
<EmptyState icon="list-checks" title="Sin rutinas todavía" description="Escanea las máquinas de tu gimnasio y te armamos una." action="Escanear máquinas" />
```

The empty state never uses the primary amber button — that weight belongs to the screen's real CTA.
