Grouping surface — 16px radius, hairline border, near-invisible drop. Use `accent` only for the AI suggestion block.

```jsx
<Card><h3>Pecho y tríceps</h3></Card>
<Card variant="accent" padding="lg">Sugerencia de rutina</Card>
```

Variants: default, raised (modals/sheets), accent (amber tint), outline (empty states).
