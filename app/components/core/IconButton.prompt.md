Icon-only control for headers, toolbars and steppers. Always pass `label`.

```jsx
<IconButton icon="bell" label="Notificaciones" />
<IconButton icon="plus" label="Añadir serie" variant="solid" />
```

`active` swaps to the soft-amber selected skin. Sizes sm 32 / md 40 / lg 48.
