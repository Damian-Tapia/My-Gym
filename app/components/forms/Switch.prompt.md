Pill toggle, amber when on. Only for settings that apply immediately — never as a form field that needs saving.

```jsx
<ListRow title="Recordatorios" trailing={<Switch checked={on} onChange={setOn} />} />
```
