Large plus/minus control with a mono numeral between it — the primary way weight and reps are edited mid-set.

```jsx
<Stepper value={40} step={2.5} unit="kg" size="lg" onChange={setWeight} />
```

Use `size="lg"` on the active-workout screen (one-handed, gloves on) and `md` inside sheets and lists.
