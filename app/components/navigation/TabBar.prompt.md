Translucent blurred bottom bar, four tabs, amber for the active one. Defaults to Hoy / Rutinas / Progreso / Perfil.

```jsx
<TabBar value={tab} onChange={setTab} />
```

Always pinned to the bottom of the screen frame; never scrolls with content.
