Blurred top bar. `large` gives the iOS-style oversized title used on root screens; compact form is for pushed views.

```jsx
<NavBar large title="Hoy" subtitle="Empuje · 45 min" action={<IconButton icon="bell" label="Avisos" />} />
```
