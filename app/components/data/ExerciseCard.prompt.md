The routine's list unit — icon tile, name, machine, and a mono "3 × 10" prescription line.

```jsx
<ExerciseCard name="Prensa de piernas" machine="Prensa 45°" sets={4} reps={10} aiSuggested status="active" />
```

`status="done"` drops the card to 60% and swaps the icon to a green tick.
