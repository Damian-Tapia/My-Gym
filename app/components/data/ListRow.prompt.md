Flat row with a bottom hairline; groups of rows sit directly on the app background, not inside a Card.

```jsx
<ListRow title="Unidades" subtitle="Kilogramos" chevron onClick={open} />
```

Remove the hairline on the last row of a group with `style={{borderBottom:'none'}}`.
