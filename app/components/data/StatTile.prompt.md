Small card holding one number in mono type, with an uppercase micro label above it.

```jsx
<StatTile icon="flame" label="Racha" value={12} unit="días" delta="+3 esta semana" />
```

Lay out in a two-column grid with `gap: var(--gap-stack)`. Never more than four per screen.
