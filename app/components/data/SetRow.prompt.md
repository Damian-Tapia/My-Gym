Row of one set inside an exercise card during a workout. Tapping marks it done.

```jsx
<SetRow index={2} weight={40} reps={10} active onToggle={complete} />
```

Exactly one row in a stack should be `active`; completed rows fade to 55% opacity.
